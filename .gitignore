# IDE metadata
.idea/
.vscode/
*.iml
*.iws
*.ipr

# Node package managers and caches
node_modules/
.pnpm-store/

# Frontend build caches and output
.angular/
frontend/.angular/
frontend/dist/

# Backend build output
backend/target/
target/

# Local environment files
.env
.env.*
!.env.example

# Spring reads ./config/application.properties from the working directory and
# lets it override the packaged one. It is where local credentials live - mail
# passwords, anything machine-specific - so it must never be committed.
config/

# OS files
.DS_Store
Thumbs.db
